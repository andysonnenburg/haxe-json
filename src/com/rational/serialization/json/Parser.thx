package com.rational.serialization.json;

com.rational.utils.Stream;

class Parser {	
	public function new() {}
	
	public function parse(stream:Stream<Token>, clazz:Class<Dyname>):Dynamic {
		while (true) {
			switch (state) {
				case S.START:
				case S.START_OBJECT:
				case S.START_ARRAY:
			}
		}
	}
}
